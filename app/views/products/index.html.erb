<h1>Product All</h1>
<% @products.each  do |product| %>
  <div>
    <h2>Name: <%= link_to product.name, product %></h2>
    <div>
      Price: <%= product.price %>$</br>
      Category: <%= product.category.name %>
    </div>
  </div>
<% end %>
